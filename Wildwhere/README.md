# WildWhere 2.0 — Wildlife Sighting Predictor
A small Flask + scikit-learn app that predicts likely wildlife by park, month, and time of day.
Run `python3 app.py` then open `web/index.html` to try it.

